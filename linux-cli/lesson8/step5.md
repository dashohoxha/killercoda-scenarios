# Package Management

`apt update`{{execute}}

`apt upgrade`{{execute}}

`apt list emacs*`{{execute}}

`apt show emacs`{{execute}}

`apt install emacs`{{execute}}

`emacs`{{execute}}

`apt remove emacs`{{execute}}
